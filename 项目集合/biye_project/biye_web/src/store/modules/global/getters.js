const getters = {
  periodStatus: state => state.periodStatus,
  addRoutes: state => state.addRoutes,
  role: state => state.role,
  permission_routes: state => state.routes,
  username: state => state.username,
};

export default getters;
